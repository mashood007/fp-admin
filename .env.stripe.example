# ===========================================
# Stripe Configuration
# ===========================================
# Get your API keys from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_your_secret_key_here
STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key_here

# Get webhook secret after creating webhook endpoint
# In Stripe Dashboard: Developers > Webhooks > Add endpoint
# Point it to: https://yourdomain.com/api/webhooks/stripe
# Events to listen to: checkout.session.completed, checkout.session.expired
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# ===========================================
# Application URLs
# ===========================================
# Frontend store-front URL - used for success/cancel redirects
STORE_FRONT_URL=http://localhost:3001

# Base URL for the admin backend (used in email links, etc.)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# ===========================================
# Existing Environment Variables
# ===========================================
# Add your existing environment variables below this line
# (DATABASE_URL, JWT_SECRET, etc.)
